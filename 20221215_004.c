#include <stdio.h>
int main()
{
    float altura, pesoideal;
    printf("altura em metros:");
    scanf("%f",&altura);

    pesoideal=(72,7*altura)-58;
    printf("o peso ideal: %f0.1", pesoideal);

    return 0
}
