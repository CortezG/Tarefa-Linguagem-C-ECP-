#include <stdio.h>
int main()
{
    float altura, pesoideal;
    printf("altura em metros:");
    scanf("%f",&altura);

    pesoideal=(72,7*altura)-58;
    printf("o peso ideal �: %f.1f", pesoideal);

    return 0
}
