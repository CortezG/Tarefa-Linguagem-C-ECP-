#include <stdio.h>

int main()
{
     float lado, 2area;

     printf(“\n medida do lado do quadrado para calculo da area: ”);
     scanf(“%f”, lado);

     2area= 2*(lado**2);

     printf(“\n O dobro da medida da area eh: %2.f”, 2area);

     return 0;
}
